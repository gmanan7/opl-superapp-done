// Thin compat loader (D-019). The single source of visual truth is
// docs/design/tokens.tailwind.ts (D-018) — consumed verbatim via the
// @config directive in src/index.css. Do not add tokens here.
import fulcrumTokens from '../docs/design/tokens.tailwind.js';
export default fulcrumTokens;
