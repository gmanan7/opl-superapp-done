# Fulcrum — Session State (D-015)

**This file is overwritten and committed at the end of every build session. It carries STATE, never decisions (those live in `DECISIONS.md`).**

---

## Current position

**Phase 3 (re-ground KPIs · OPL · Kaizen) — ALL THREE COMPLETE + shipped/live on `origin/main`.** Each was rebuilt on the new foundation (D-024 tiered access, SECURITY DEFINER RPCs closing the §F.4 broken-access class, design-system surfaces, four-language i18n + content translation, §F gate, live role-impersonation probes).

- **Module 3 — Kaizen: COMPLETE (backend + frontend + hardening pass, pushed). D-030 committed (`a9e74ab`).**
  - **Backend** (`docs/sql/migrations/p3_m3_kaizen.sql` + `p3_m3_kaizen_storage_hygiene.sql`, non-contract; live §F probes **27/27** `scripts/p3_m3_kaizen_probe.py` + image Edge **5/5** `scripts/p3_m3_kaizen_edge_probe.py`):
    - Access: D-024 tiered reads; own-group draft create + author content-UPDATE; **status/score/approval REVOKEd from client → SECURITY DEFINER RPCs only** (closed the live hole where any factory user could approve/score any kaizen).
    - `can_approve_kaizen_group` = **D-020 approver tier** {dmt_leader→DMT groups, pillar_champion/be_team/admin→factory}; **jh_leader/dmt_member = reviewer (read-only)** — corrects the prototype's wrong jh_leader+ gate. RPCs: `submit_kaizen` (author), `approve_kaizen(pq,ehs,quant,easy)` [**total_score = straight SUM /36 computed SERVER-SIDE**; 1/3/9 validated], `reject_kaizen(reason)`.
    - Hygiene: dropped 10 verified-empty residue columns + `kaizen_training_record` (prototype residue, 0 rows); reduced to **1 before + 1 after** image slot + per-slot hash (D-027); GIN on `team_member_ids`.
    - §F image: **`validate-kaizen-image` Edge** (author-only; magic-byte/double-ext/allow-list/size; stores PATH+sha256) + `kaizen_orphan_image_paths` RPC + **`opl-orphan-sweep` extended to `*/kaizen/*`** (one daily cron). **Retired `getPublicUrl` for kaizen.**
  - **Frontend** (re-grounded on tokens): `src/lib/kaizen.ts` (pure sum scoring, tested); `useKaizen` (RLS reads, RPC lifecycle, auth from `src/lib/auth`); `uploadKaizenImageViaEdge`. KaizenList/IdeaForm/FullForm/Detail rebuilt (CaptureColumn, StatusBadge, BeforeAfterImagePair, TranslateControl, ConfirmModal). Two-stage idea→full→approved/rejected+scored. Detail scoring panel: **approver-tier editable (score + approve in ONE RPC, live /36) · reviewer read-only · worker "awaiting approval"**. Team multiselect; cost + benefit captured. **TranslateControl on problem/solution/HD/benefit prose.** i18n `kaizen` 89 keys ×4 — **hi/gu now REAL (were English placeholders), ta drafted (VERIFY)**; four-way parity; all `t()` keys resolve; zero hardcoded English.
- **Module 2 — OPL: M2a (lesson+security) + M2b (training) COMPLETE.** Group-tiered RLS + lifecycle/training RPCs; event-grained `opl_training_event` + audience override + due/retrain (cap-5) + soft-retire; §F `validate-opl-image` + D-027 sweep; four-language. Legacy `opl_training_record` dropped (D-029). Detail in git history + D-028/D-029.
- **Module 1 — KPIs: COMPLETE.** D-024 group-tiered access; capture-cards surfaces; faster-than-paper levers met. Detail in git history.
- **Build:** zero TS errors. **Tests:** 340/340 (M3 added 5 scoring tests `src/__tests__/kaizen.test.ts`). **ESLint:** clean (2 pre-existing errors in ConfirmModal/RosterFilterBar untouched). Zero missing-key warnings.
- **Contract:** untouched — `kaizen`/`opl`/`opl_training_event`/`opl_intended_audience` + the `jh_kpi_*` tables are all NON-contract; no master-table touch; no contract run required. Backend applied directly via the Management API + probed; Edges CLI-deployed (per D-026; SOURCE on `origin/main`).

## Standing engineering rules (owner-directed)
1. **Never use native `user_role` enum ordering in SQL** — `dmt_member`/`dmt_leader` are SWAPPED in the Postgres enum. App ladder (`src/lib/auth` `roleAtLeast`) is the truth; explicit role lists in RLS/RPCs. (Note: `roleAtLeast(role,'dmt_leader')` = exactly the D-020 Kaizen approver tier.)
2. Types mirroring a DB/RPC/view shape are schema CLAIMS — verify against the real schema (M3: live `information_schema` after the hygiene drop, before writing hooks).
3. Deleting an i18n namespace/key requires a live-reference grep first.
4. Round-trip / behavioral proof, not "the call fired" (M3 RLS/RPC proven by 27/27 + image Edge 5/5 live probes).
5. Display names ALWAYS via `worker_names` (email) / `tpm_session.worker.name` (PIN).

## STATUS hygiene rule (standing)
Key-completeness ≠ translation-completeness. Per-namespace state verified by value comparison. Baseline: `docs/i18n_audit_phase2_step1.md` §1.

## Per-namespace translation state
**en/hi/gu** ✓ translated-reviewed: common, home, login, mdm, mdmImport, nav, patterns, people, roles, syncHealth, translate, kpi, opl (97 keys), **kaizen (89 keys — M3: hi/gu re-translated from English placeholders)** — ⏳ deferred: abn.
**ta** ✓ drafted: the Phase-2 namespaces + kpi + opl + **kaizen (89 keys)** — VERIFY-flagged rows pending Tamil-helper review (kpi, opl, **and the full kaizen namespace**). *absent* for abn. ta SELECTABLE (D-008 closed).

## Open flags — carry forward (explicit, non-blocking)
- **Kaizen UAT round 2 — CLOSED (code); awaiting owner's on-phone confirmation (D-025):**
  - **(D) image upload — CONFIRMED ROOT = CORS, one header deeper, FIXED.** Owner's browser capture: preflight rejected **`x-factory-id`** ("not allowed by Access-Control-Allow-Headers"). The PIN client injects `x-factory-id`+`x-worker-id` via `pinAwareFetch` (`src/lib/supabase.ts`) on every invoke; the round-1 Allow-Headers listed `x-worker-id` but **not** `x-factory-id` → `FunctionsFetchError`, request never reached the function (nothing stored). The compression/body-size theory was WRONG (reproduced: the Edge accepts a 10.7 MB body → clean 422/200, no ceiling — `scripts/p3_repro_upload.py`). **Systemic fix:** the shared `_shared/cors.ts` OPTIONS preflight now **ECHOES `Access-Control-Request-Headers`** (default-list incl. `x-factory-id` fallback) → every current/future custom header passes; redeployed all 4 browser edges (incidentally fixes OPL's upload — shared edge; no OPL UI touched). **Verification de-falsed:** `scripts/p3_cors_probe.py` now sends the FULL derived client header set and asserts Allow-Headers covers EVERY one (incl `x-factory-id`) → 24/24. §F unchanged (image 5/5, RLS 27/27). Upload helper also instrumented (exact-error surfacing) + re-compress-under-256 KB guard.
  - **(A/B/C) Kaizen UI closed on the working upload:** (A) **single-form create/edit** `KaizenForm` (all fields one screen; photo on form mobile+desktop; **title + brief + problem + solution ALL mandatory to submit** — client Submit-gate + server `submit_kaizen` guard, sanitized; draft saves incomplete; create-first) — routes `/kaizen/new` + `/kaizen/:id/edit`; **removed** `/promote` + `KaizenIdeaForm`/`KaizenFullForm`; **reverses the two-stage element of D-030 → D-031 committed (owner-approved).** (B) image preview `aspect-[4/3]` + `object-contain` (fits, no crop/overflow). (C) factory-wide team picker via **`kaizen_worker_search`** (minimal `{id,name,jh_group}`, factory-scoped SECURITY DEFINER — survives the People/PIN tightening; no broad `worker_profile` read; no cross-factory).
  - **OWNER GATE (D-025):** real on-phone Kaizen photo upload now lands + displays; the single-form create→submit→approve flow in four languages. Re-test OPL upload under the OPL pass (the shared CORS/header fix covers it).
- **Hub OPL export (PARALLEL track) — AWAITING the Fulcrum Training Hub orchestrator's reply (carry):** Hub-PULL `opl_training_feed` view proposed (off the master sync; maps `opl_training_event`→Hub sessions/attendance; frozen+additive). M2b is export-ready; on reply, add the feed + record its own D-record.
- **Owner visual checks — PENDING (D-025 owner's gate; perceptual, NOT machine-checkable):**
  - **OPL (M2a/M2b):** four-language render of OPL list/detail + `/opl/training` on a phone; TranslateControl on a real hi/gu remark; create→submit→approve; self-ack + leader mark/audience/retire.
  - **Kaizen (M3, NEW):** four-language render of KaizenList/IdeaForm/FullForm/Detail on a phone; the **scoring panel /36** + approve/reject as a dmt_leader+; idea→full→submit→approve flow; TranslateControl on a real hi/gu problem/solution; team multiselect; **in-browser** before/after image upload + draft display (confirms the CORS fix end-to-end); **translate a real hi/gu field in-browser**; **no horizontal scroll on a phone**; edit/approve/print/share visible on desktop.
- **Kaizen (M3) new flags:**
  - **Print = `window.print()`** of the detail (no dedicated A4 one-pager). A Kaizen print one-pager (like OPLPrintView) is **deferred** (KAIZEN_DETAIL §12 notes it as a separate deliverable).
  - **Cross-group audience-style team add:** team multiselect lists the OPL/kaizen group's active workers; cross-group support members aren't in the picker (matches the group scope; broaden later if needed).
  - **D-030 — committed (`a9e74ab`, owner-approved):** Kaizen re-ground realizes D-020/§F.4 — server-enforced approver tier; /36 server-scoring; team/cost/benefit; §F image gate; schema-hygiene drops. Non-contract.
  - **Edge CORS hardening (M3 hardening pass):** shared `supabase/functions/_shared/cors.ts` — **origin-ALLOW-LISTED (C1, never `*`), prod HARDCODED so it can't break if `ALLOWED_ORIGINS` is unset, + ACAM + 204 OPTIONS preflight + CORS on EVERY path** — applied to `validate-kaizen-image`, `validate-opl-image`, `translate-content`, `sign-url`. Root: image edges + sign-url had ACAO but **no Access-Control-Allow-Methods**, OPTIONS=200 (not 204), and sign-url dropped CORS on errors → a browser fetch can fail while the function logs 200 (direct-HTTP probes were false-green). **Incidentally fixed the shared `validate-opl-image` + `sign-url` edges' CORS (helps OPL; no OPL UI touched).** ADDITIVE — §F image validations + auth UNCHANGED (live: CORS probe 24/24 `scripts/p3_cors_probe.py`; image Edge 5/5; RLS 27/27). **`translate-content` CORS was already correct (ACAO echoed prod) → any translate failure is NON-CORS** — check `GOOGLE_TRANSLATE_SA_KEY`/`GOOGLE_PROJECT_ID` secrets, the `content_translation` kill-switch, or the monthly budget.
- **Deferred OPL follow-ups (own steps):** **designated-trainer capability** (per-OPL grant for specific workers incl. apprentices/seniors — parked D-010-style); **`opl_training_feed`** Hub export (after the Hub orchestrator confirms its model). Also: ta VERIFY rows (kpi+opl+kaizen) pending the Tamil-helper review.
- **Carried tech-debt/flags:** `TranslateControl` `originalLang` defaults to source-detect / `hi` (no per-field source stored) → v1.2 persist per field; **abnormality is now the ONLY module still on `getPublicUrl`** (kaizen fixed in M3) — fix when abnormality re-grounds (Phase 4; OPL/Kaizen `validate-*-image` + path-store is the template); **`useAbnormalities` compat-shim now used ONLY by abnormality** (kaizen repointed to `src/lib/auth` in M3) — remove the shim when abnormality re-grounds; multi-KPI dual-axis trend → Phase 5; KPI/unit glossary transliteration → DATA workstream; PATTERNS v1.2 (CaptureColumn/MachineCard/TrendSection ratification; ScoreSegment/ScoringPanel/TeamMemberList were built inline in M3, not promoted to shared patterns — candidate for PATTERNS v1.2); `manage-factory` Edge backlog; seed-password rotation; SECURITY_VAPT B9 (signed PIN tokens).
- **Carried from Phase 2:** Bulk Import UI UAT; `ta_review.xlsx` Tamil review; Supabase Auth session caps → Phase 6; glossary `ta` populate+push; Home hardcoded-English (Overview/StatCard) → Home re-ground; `translation_cache` retention/DPDP → Phase 6; D-023 data-residency doc → Phase-6 VAPT pack.
- **Test residue:** `TZZ-DRYRUN` factory inactive, 0 workers (D-005); `ZUAT-S8-*` + dryrun machines known residue (CLAUDE.md baseline 2026-06-14).

## Next step
**Phase 3 module re-ground is done.** Next, in order: (1) deferred OPL — **designated-trainer capability** step; **`opl_training_feed`** Hub export once the Hub orchestrator replies (thread 1); (2) **Phase 4 — complete the JH pillar:** Abnormality completion (title/crew_type/closure rules + re-ground its `getPublicUrl`→`validate-abn-image` + drop the `useAbnormalities` shim) → CLTI → JH Meetings → JH Audit; each §F-gated. Owner visual gates for OPL + Kaizen are outstanding (above).
